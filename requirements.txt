# Dependências da APP (usadas no deploy do Streamlit Cloud).
# As ferramentas pesadas de transcrição estão em requirements-ingest.txt
# (só necessárias localmente para processar os vídeos).
 
python-dotenv==1.0.1
 
# LLM, RAG e agente
openai==1.30.5
httpx<0.28          # a 0.28 removeu 'proxies', que o openai 1.30.5 ainda usa
langchain==0.2.5
langchain-openai==0.1.8
langchain-community==0.2.5
chromadb==0.5.0
tiktoken==0.7.0
 
# Avaliação / tracing
langsmith==0.1.77
 
# Interface
streamlit==1.35.0
streamlit-mic-recorder==0.0.8
 
pydantic==2.7.4
 